


digraph G {
    compound=true;
    rankdir="LR";
    
    subgraph interfaces {
        node[color=green,shape=square, style=rounded, style=dashed];
        Database;
        Table;
        Datasource;
        Warnable;
        Taggable;
        Field;
        // Ownable
        // Certifiable
    }
    
    Database -> {CloudFile, DatabaseServer, File, WebDataConnector } [dir=back];
    Table -> {DatabaseTable, CustomSqlTable} [dir=back];
    Datasource -> {PublishedDatasource, EmbeddedDatasource}[dir=back];
    View -> {Sheet, Dashboard}

    Warnable -> {CloudFile, DatabaseServer ,File, WebDataConnector, DatabaseTable, PublishedDatasource, Flow}[dir=back, color=blue, style=dotted];
    Taggable -> {CloudFile, DatabaseServer ,File, WebDataConnector, DatabaseTable, PublishedDatasource, Flow}[dir=back, color=purple, style=dotted];


    // abstracts
    Field [shape=ellipse];
    Datasource [shape=ellipse];
    Table [shape=ellipse];
    View [shape=ellipse];

    # parents and children
    TableauSite -> User, TableauProject [label="contains", color=red, style=dashed]
    TableauProject -> {Workbook, Metric, Flow, Datasource} [label="contains", color=red, style=dashed]
    Datasource -> {DataSourceFilter, Field}[label="contains", color=red, style=dashed]
    DatabaseServer -> {CloudFile, Table, File, WebDataConnector}[label="contains", color=red, style=dashed]
    Workbook -> {EmbeddedDatasource, View} [label="contains", color=red, style=dashed]
    Table -> Column[label="contains", color=red, style=dashed]
    EmbeddedDatasource -> Field[label="contains", color=red, style=dashed]
    
    DatabaseServer -> PublishedDatasource, Workbook, View, Metric, Flow [label="refers to",style = dashed; color=green]
    Flow ->  Field[label="refers to", style = dashed; color=green]
    Sheet -> Field [label="refers to", style = dashed; color=green]
    Metric -> Sheet[label="refers to", style = dashed; color=green]
    Column -> Field [label="refers to", style = dashed; color=green]
    Dashboard -> Sheet [label="refers to", style = dashed; color=green]
    DataSourceFilter -> Field [label="refers to", style = dashed; color=green]
    Workbook -> PublishedDatasource [label="refers to", style = dashed; color=green]
    User


    //I don't understand where a workbook/anything refers to a Database
    // Is a databaseServer the same thing as a dataConnection?

    label="Tableau Metadata API"
    
}
